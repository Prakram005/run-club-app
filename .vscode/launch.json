{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run Club API",
      "cwd": "${workspaceFolder}/run-club/server",
      "program": "${workspaceFolder}/run-club/server/index.js",
      "envFile": "${workspaceFolder}/run-club/server/.env",
      "console": "integratedTerminal",
      "skipFiles": [
        "<node_internals>/**"
      ]
    }
  ]
}
